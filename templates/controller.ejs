
const <%= name %> = require('service/<%= path %>/<%= name %>.js')

module.exports = async (ctx, next) => {
  let respond = []
  //获取参数
  let { consumptionName, consumptiontypeid } = ctx.request.body;
  //处理参数
  //ctx.request.body = {}
  //业务层处理
  let serviceResult = await <%= name %>(ctx, next)
  response(ctx, respond,{message:'添加成功'})
}
